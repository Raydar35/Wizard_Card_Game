<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <groupId>com.wizbiz</groupId>
    <artifactId>Wizard_Card_Game</artifactId>
    <version>1.0-SNAPSHOT</version>
    <name>Wizard_Card_Game</name>

    <properties>
        <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
        <junit.version>5.10.2</junit.version>
        <javafx.version>22.0.2</javafx.version>
        <!-- Default platform classifier. This will be overridden by OS-activated profiles below. -->
        <javafx.platform.classifier>win</javafx.platform.classifier>
        <!-- Compilation level: set to the Java version your developers use. Change if your team standard is different. -->
        <maven.compiler.source>17</maven.compiler.source>
        <maven.compiler.target>17</maven.compiler.target>
        <maven.compiler.release>17</maven.compiler.release>
    </properties>

    <dependencies>
        <!-- JavaFX dependencies use a platform-specific classifier provided by the active Maven profile -->
        <dependency>
            <groupId>org.openjfx</groupId>
            <artifactId>javafx-controls</artifactId>
            <version>${javafx.version}</version>
            <classifier>${javafx.platform.classifier}</classifier>
        </dependency>
        <dependency>
            <groupId>org.openjfx</groupId>
            <artifactId>javafx-fxml</artifactId>
            <version>${javafx.version}</version>
            <classifier>${javafx.platform.classifier}</classifier>
        </dependency>
        <dependency>
            <groupId>org.openjfx</groupId>
            <artifactId>javafx-graphics</artifactId>
            <version>${javafx.version}</version>
            <classifier>${javafx.platform.classifier}</classifier>
        </dependency>
        <dependency>
            <groupId>org.kordamp.ikonli</groupId>
            <artifactId>ikonli-javafx</artifactId>
            <version>12.3.1</version>
        </dependency>
        <dependency>
            <groupId>org.junit.jupiter</groupId>
            <artifactId>junit-jupiter-api</artifactId>
            <version>${junit.version}</version>
            <scope>test</scope>
        </dependency>
        <dependency>
            <groupId>org.junit.jupiter</groupId>
            <artifactId>junit-jupiter-engine</artifactId>
            <version>${junit.version}</version>
            <scope>test</scope>
        </dependency>
    </dependencies>

    <build>
        <plugins>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-compiler-plugin</artifactId>
                <version>3.13.0</version>
                <configuration>
                    <!-- Use the shared properties so developers can adjust the Java version in one place -->
                    <source>${maven.compiler.source}</source>
                    <target>${maven.compiler.target}</target>
                    <release>${maven.compiler.release}</release>
                </configuration>
            </plugin>
            <plugin>
                <groupId>org.openjfx</groupId>
                <artifactId>javafx-maven-plugin</artifactId>
                <version>0.0.8</version>
                <executions>
                    <execution>
                        <id>default-cli</id>
                        <configuration>
                            <!-- Use modular mainClass: moduleName/fully.qualified.MainClass -->
                            <mainClass>com.wizbiz.wizard_card_game/com.wizbiz.wizard_card_game.GameUI</mainClass>
                            <launcher>app</launcher>
                            <jlinkZipName>app</jlinkZipName>
                            <jlinkImageName>app</jlinkImageName>
                            <noManPages>true</noManPages>
                            <stripDebug>true</stripDebug>
                            <noHeaderFiles>true</noHeaderFiles>
                            <!-- macOS compatibility options (kept as-is) -->
                            <options>
                                <option>-Dprism.order=sw</option>
                                <option>-Dprism.verbose=true</option>
                                <option>-Djava.awt.headless=false</option>
                                <option>-Dprism.forceGPU=false</option>
                                <option>-Djavafx.animation.fullspeed=true</option>
                                <option>--add-opens=javafx.graphics/com.sun.glass.ui=ALL-UNNAMED</option>
                                <option>--add-opens=javafx.graphics/com.sun.glass.ui.mac=ALL-UNNAMED</option>
                                <option>--add-exports=javafx.graphics/com.sun.glass.ui=ALL-UNNAMED</option>
                                <option>--add-exports=javafx.graphics/com.sun.javafx.application=ALL-UNNAMED</option>
                            </options>
                        </configuration>
                    </execution>
                </executions>
            </plugin>
        </plugins>
    </build>

    <!-- OS-activated profiles to select platform-specific JavaFX native classifier. -->
    <profiles>
        <profile>
            <id>windows</id>
            <activation>
                <os>
                    <family>Windows</family>
                </os>
            </activation>
            <properties>
                <javafx.platform.classifier>win</javafx.platform.classifier>
            </properties>
        </profile>

        <profile>
            <id>linux</id>
            <activation>
                <os>
                    <family>unix</family>
                </os>
            </activation>
            <properties>
                <javafx.platform.classifier>linux</javafx.platform.classifier>
            </properties>
        </profile>

        <profile>
            <id>mac</id>
            <activation>
                <os>
                    <family>mac</family>
                </os>
            </activation>
            <properties>
                <javafx.platform.classifier>mac</javafx.platform.classifier>
            </properties>
        </profile>

        <profile>
            <id>mac-aarch64</id>
            <activation>
                <os>
                    <family>mac</family>
                    <arch>aarch64</arch>
                </os>
            </activation>
            <properties>
                <javafx.platform.classifier>mac-aarch64</javafx.platform.classifier>
            </properties>
        </profile>
    </profiles>

</project>
